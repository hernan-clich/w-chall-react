import React from 'react';

import { StyledFooter } from '../../styles/Home/Footer';

const Footer = () => {
  return (
    <StyledFooter>
      <p>Footer</p>
    </StyledFooter>
  );
};

export default Footer;
