export const primary = (alpha = 1) => `rgba(0, 169, 229, ${alpha})`;

export const secondary = (alpha = 1) => `rgba(151, 207, 0, ${alpha})`;

export const plainWhite = (alpha = 1) => `rgba(255, 255, 255, ${alpha})`;

export const outerSpacing = (space = 5) => `${space}em`;